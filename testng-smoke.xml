<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<!--
  testng-smoke.xml — SMOKE suite
  Runs only tests tagged with group "smoke": 6 critical-path sanity checks.
    TC_GH_001  GitHub — authenticate and fetch profile
    TC_RB_001  Booker  — generate auth token
    TC_RB_002  Booker  — create booking (prerequisite for later tests)
    TC_PF_001  Platzi  — login and obtain JWT
    TC_WM_001  WireMock — GET all bookings via file-based stub
    TC_SD_001  StubDemo — GET /api/items via programmatic stub
  Execute: mvn test -DsuiteXmlFile=testng-smoke.xml
-->
<suite name="REST Assured API Test Suite — Smoke" verbose="2" parallel="false">

    <listeners>
        <listener class-name="org.testng.reporters.JUnitReportReporter"/>
    </listeners>

    <!-- ── GitHub OAuth 2.0 ── -->
    <test name="GitHub API Tests — Smoke" preserve-order="true">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="com.apiframework.tests.github.GitHubTests"/>
        </classes>
    </test>

    <!-- ── Restful-Booker Cookie Token ── -->
    <test name="Restful-Booker API Tests — Smoke" preserve-order="true">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="com.apiframework.tests.booker.BookerTests"/>
        </classes>
    </test>

    <!-- ── Platzi Fake Store JWT ── -->
    <test name="Platzi Fake Store API Tests — Smoke" preserve-order="true">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="com.apiframework.tests.platzi.PlatziTests"/>
        </classes>
    </test>

    <!-- ── WireMock (embedded mock server) ── -->
    <test name="WireMock Tests — Smoke" preserve-order="true">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="com.apiframework.tests.wiremock.WireMockTests"/>
        </classes>
    </test>

    <!-- ── Pure programmatic stub demonstration ── -->
    <test name="Stub Demo Tests — Smoke" preserve-order="true">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="com.apiframework.tests.stub.StubDemoTests"/>
        </classes>
    </test>

</suite>
